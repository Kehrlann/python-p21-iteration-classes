def taxes(income):
    """
    Compute taxes from income

    Parameters:
      income: the income from which to compute the taxes
    Returns:
      an int representing the taxes, rounded down
    """
    return -1
